---
title: Python SDK v0.4.1
---

# Python SDK v0.4.1

Released: 2026-07-18

### Breaking Changes

- **Removed .** The server hard-discards this field ( is always null server-side); offline loops live inside . The SDK also strips  from create/update payloads so personas rebuilt from older API responses never send it.

### Features

- **Eye-tracking data access**:  with the standard pagination/output options, plus .  now includes an  key.
- **Room variables**:  — the reconstructed shared-variable timeline: .
- **Participant counts**:  — cheap per-data-type document counts ( / ).
- **Experiment export**:  — portable experiment JSON, suitable for re-import.
- **Cognition catalog**:  — the valid building blocks for persona  configs (, , ). Requires the  scope.

*Note: this release was briefly published as 0.5.0, which has been yanked on PyPI — 0.4.1 is the canonical version.*
