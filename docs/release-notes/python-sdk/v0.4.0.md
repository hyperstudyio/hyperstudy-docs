---
title: Python SDK v0.4.0
---

# Python SDK v0.4.0

Released: 2026-07-17

## Features

- **Agent data access**:  (per-turn decision logs plus run-manifest rows),  (single decision with full detail blobs: prompt, reasoning chain, peer-model snapshot, prediction update), and  (run manifests with orphaned-run flags). Emits a warning when the server truncates decisions at the per-room limit.
-  and  enum entries;  now includes an  key.
- **Agent-role experiment authoring**:  accepts  and ; new  (per-role , , ) and  models;  gains  and  fields.
- **Persona management**: , , ,  (merge-patch), , , with typed  /  builders. Requires API-key scopes  / .
- **Agent deployment launch & control**:  (including agent-only deployments — rooms launch server-side immediately), , , , . Requires the  scope.

## Fixes

-  now matches the packaged version (was stuck at 0.3.0 while pyproject said 0.3.1).
